🔹 1. EC2 Initial Setup (Ubuntu)
     - sudo apt update -y

     - sudo apt install apache2 php php-mysql mysql-client -y

     - sudo systemctl start apache2

     - sudo systemctl enable apache2

🔹 2. Web Application Setup
     - cd /var/www/html

     - sudo rm index.html

     - sudo nano index.php

     - sudo chown -R www-data:www-data /var/www/html

     - sudo chmod -R 755 /var/www/html

     - sudo systemctl restart apache2

🔹 3. Connect EC2 to RDS MySQL
     - mysql -h <RDS-ENDPOINT> -u <DB-USERNAME> -p


🔹 4. Create table using database
     - show databases;

     - use intel;

     - create table data (firstname varchar(20), email varchar(20));

     - insert into data values ('AWS', 'support@aws.com');

     - select * from data;

     - exit

🔹 5. Add PHP repository
     - sudo add-apt-repository -y ppa:ondrej/php

     - sudo apt update

🔹 6. Test database connection
    - sudo mysql -h <RDS_ENDPOINT> -u <USERNAME> -p

    - use intel;

    - select * from data;

🔹 7. Access via browser

    - Open your EC2 Public IP or ALB DNS in browser

    - PHP page should display data from RDS







